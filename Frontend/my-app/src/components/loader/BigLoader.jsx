import React from "react";
import Loader from "./Loader";

const BigLoader = () => {
    return (
        <div id="loaderWrapper">
            <Loader size={150} />
        </div>
    );
};

export default BigLoader;
